#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> s;

    int n;
    int i;
    cin >> n;

    for(int j=0; j<=n; j++){
        cin >> i;
        s.insert(i);
    }

}